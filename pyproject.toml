[project]
name = "vaultix"
version = "0.1.0"
description = "A secure desktop password management application"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "argon2-cffi>=25.1.0",
    "cryptography>=50.0.1",
    "pyside6>=6.11.2",
    "sqlalchemy>=2.0.52",
]

[tool.uv]
package = false